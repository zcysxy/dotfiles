

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
if ( -f "/opt/homebrew/anaconda3/etc/profile.d/conda.csh" ) then
    source "/opt/homebrew/anaconda3/etc/profile.d/conda.csh"
else
    setenv PATH "/opt/homebrew/anaconda3/bin:$PATH"
endif
# <<< conda initialize <<<

